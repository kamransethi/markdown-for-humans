/**
 * Stub extension module for slash command support.
 *
 * The actual slash command functionality is not currently implemented in this repository,
 * but the module exists so webview tests and build imports can resolve the path.
 */

export const SlashCommand = {
  name: 'slashCommand',
};

export default SlashCommand;
